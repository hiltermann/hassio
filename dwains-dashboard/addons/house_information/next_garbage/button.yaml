type: custom:button-card
entity: sensor.next_garbage
template: rooms_child
name: Next Garbage
icon: mdi:trash-can-outline
label: >
  [[[ 
    if(entity){
      return entity.state;
    } else {
      return 'Entity error!';
    }
  ]]]
